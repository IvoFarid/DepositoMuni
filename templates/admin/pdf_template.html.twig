<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8">
		<title>Constancia de envío</title>
	</head>
	<body style="font-family: Arial, sans-serif;">
		<div style="page-break-inside: avoid; display: block; border: 1px solid #000; padding: 20px; margin: 0 auto;">
			<header style="text-align: center; margin-bottom: 20px;">
				<h1 style="margin: 0; font-size: 24px;">Constancia de envío</h1>
				<p style="margin: 5px 0; font-size: 14px;">
          <span id="fecha-actual">{{ constancia.getDate()|fecha_localizada }}</span>
				</p>
			</header>
			<section style="margin-bottom: 10px;">
				<h2 style="margin: 0 0 10px 0; font-size: 18px;">Inventario enviado a: {{ constancia.getSendedTo() }}</h2>
				<table style="width: 100%; border-collapse: collapse;">
          <thead>
            <tr>
              <th style="border: 1px solid #000; padding: 8px; text-align: left;">Producto</th>
              <th style="border: 1px solid #000; padding: 8px; text-align: left;">Modelo</th>
              <th style="border: 1px solid #000; padding: 8px; text-align: left;">Cantidad</th>
            </tr>
          </thead>
          <tbody>
            {% for detail in details %}
            <tr>
              <td style="border: 1px solid #000; padding: 8px;">{{ detail.getProduct().getType() }} {{ detail.getProduct().getBrand()}}</td>
              <td style="border: 1px solid #000; padding: 8px;">{{ detail.getProduct().getModel() }}</td>
              <td style="border: 1px solid #000; padding: 8px;">{{ detail.getQuantity() }}</td>
            </tr>
            {% endfor %}
          </tbody>
				</table>
			</section>
			<section style="margin-bottom: 10px;">
				<h2 style="margin: 0 0 10px 0; font-size: 18px;">Detalle de productos:</h2>
        {% for detail in details %}
        <p style="margin: 5px 0 0 0">Series del {{ detail.getProduct().getType() }} {{ detail.getProduct().getBrand()}}, {{ detail.getProduct.getModel()}}:</p>
        <p style="max-width: 100%; margin: 5px 0 0 0; font-size: 14px; word-wrap: break-word; white-space: normal;">
					{{ detail.getSeries() }}
				</p>
        {% endfor %}
			</section>
				<p style="margin: 20px 0;">Firma: ___________________________</p>
			<footer style="text-align: center; font-size: 14px;">
				<p style="margin: 0 0 0 0">Secretaria de Modernización</p>
				<p>Tel: 5128544</p>
			</footer>
		</div>		
    <br>
    <div style="page-break-before: always; page-break-inside: avoid; display: block; border: 1px solid #000; padding: 20px; margin: auto auto;">
			<header style="text-align: center; margin-bottom: 20px;">
				<h1 style="margin: 0; font-size: 24px;">Constancia de envío</h1>
				<p style="margin: 5px 0; font-size: 14px;">
					<span id="fecha-actual">{{ constancia.getDate()|fecha_localizada}}</span>
				</p>
			</header>
			<section style="margin-bottom: 10px;">
				<h2 style="margin: 0 0 10px 0; font-size: 18px;">Inventario enviado a: {{ constancia.getSendedTo() }}</h2>
				<table style="width: 100%; border-collapse: collapse;">
        <thead>
					<tr>
						<th style="border: 1px solid #000; padding: 8px; text-align: left;">Producto</th>
						<th style="border: 1px solid #000; padding: 8px; text-align: left;">Modelo</th>
						<th style="border: 1px solid #000; padding: 8px; text-align: left;">Cantidad</th>
					</tr>
        </thead>
        <tbody>
          {% for detail in details %}
					<tr>
						<td style="border: 1px solid #000; padding: 8px;">{{ detail.getProduct().getType() }} {{ detail.getProduct().getBrand()}}</td>
						<td style="border: 1px solid #000; padding: 8px;">{{ detail.getProduct().getModel() }}</td>
						<td style="border: 1px solid #000; padding: 8px;">{{ detail.getQuantity() }}</td>
					</tr>
          {% endfor %}
        </tbody>
				</table>
			</section>
			<section style="margin-bottom: 10px;">
				<h2 style="margin: 0 0 10px 0; font-size: 18px;">Detalle de productos:</h2>
        {% for detail in details %}
        <p style="margin: 5px 0 0 0">Series del {{ detail.getProduct().getType() }} {{ detail.getProduct().getBrand()}}, {{ detail.getProduct.getModel()}}:</p>
        <p style="margin: 5px 0 0 0; font-size: 14px; word-wrap: break-word; white-space: normal;">
					{{ detail.getSeries() }}
				</p>
        {% endfor %}
			</section>
				<p style="margin: 20px 0;">Firma: ___________________________</p>
			<footer style="text-align: center; font-size: 14px;">
				<p style="margin: 0 0 0 0">Secretaria de Modernización</p>
				<p>Tel: 5128544</p>
			</footer>
		</div>
	</body>
</html>